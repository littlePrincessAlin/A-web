/**
 * @param {string[]} pizza
 * @param {number} k
 * @return {number}
 */
var ways = function(pizza, k) {
  let times = 0;
  for(let i = 0; i < k; i++) {
    
  }
  return times;
};
console.log(ways(["A..","AAA","..."], 3));
